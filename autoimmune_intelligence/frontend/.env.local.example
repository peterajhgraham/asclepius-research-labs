# Backend URL (server-side only — used by the Next.js API proxy route)
API_URL=http://localhost:8000

# Clerk authentication (required for production)
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=
