# Core HTTP / parsing
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=5.0.0

# Numerical / ML
numpy>=1.26.0

# Graph database (optional — only needed for Neo4j backend)
neo4j>=5.0.0

# API server (optional — only needed if running api/app.py)
flask>=3.0.0

# Notebook support
jupyter>=1.0.0
ipykernel>=6.0.0
